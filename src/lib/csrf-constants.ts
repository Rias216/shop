export const CSRF_COOKIE = "csrf_token";
export const CSRF_FORM_FIELD = "csrf_token";
/** Forwarded from proxy so layouts can read the token on the first request. */
export const CSRF_REQUEST_HEADER = "x-csrf-token";
